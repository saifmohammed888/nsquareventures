{
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check shared-system.js && node --check admin.js && node --check api/content.js && node --check api/upload.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.0"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
